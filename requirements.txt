# Core runtime requirements (mirror of pyproject [project.dependencies]).
# Prefer `pip install -e ".[dev]"`; this file exists for environments that
# expect a requirements file. Pin exact versions via constraints.txt.
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
pydantic>=2.6
pydantic-settings>=2.2
pyyaml>=6.0
typer>=0.12
rich>=13.0
