name = "start-mrt-gateway"
main = "worker.js"
compatibility_date = "2024-09-01"
account_id = "ce0ed251afb1110fd68281f824baceea"

# Route static assets directly to Cloudflare Static Assets (bypasses worker execution to preserve free-tier quota)
[assets]
directory = "../../webapp/dist"
binding = "ASSETS"

# Non-sensitive public environment variables
[vars]
ORACLE_ORIGIN_URL = "https://137.23.61.219.sslip.io"
ENVIRONMENT = "production"
